# HAProxy Configuration
# Layer 4/7 Load Balancer for Web Servers

#---------------------------------------------------------------------
# Global settings
#---------------------------------------------------------------------
global
    log stdout format raw local0 info
    maxconn 4096
    user haproxy
    group haproxy

    # Default SSL material locations
    ca-base /etc/ssl/certs
    crt-base /etc/ssl/private

    # Default ciphers to use on SSL-enabled listening sockets
    ssl-default-bind-ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES128-GCM-SHA256
    ssl-default-bind-options ssl-min-ver TLSv1.2 no-tls-tickets

#---------------------------------------------------------------------
# Common defaults
#---------------------------------------------------------------------
defaults
    mode http
    log global
    option httplog
    option dontlognull
    option http-server-close
    option forwardfor except 127.0.0.0/8
    option redispatch
    retries 3
    timeout connect 5000
    timeout client  50000
    timeout server  50000
    errorfile 400 /usr/local/etc/haproxy/errors/400.http
    errorfile 403 /usr/local/etc/haproxy/errors/403.http
    errorfile 408 /usr/local/etc/haproxy/errors/408.http
    errorfile 500 /usr/local/etc/haproxy/errors/500.http
    errorfile 502 /usr/local/etc/haproxy/errors/502.http
    errorfile 503 /usr/local/etc/haproxy/errors/503.http
    errorfile 504 /usr/local/etc/haproxy/errors/504.http

#---------------------------------------------------------------------
# Statistics page
#---------------------------------------------------------------------
listen stats
    bind *:8404
    stats enable
    stats uri /stats
    stats refresh 10s
    stats admin if LOCALHOST
    stats auth admin:admin

#---------------------------------------------------------------------
# Frontend - HTTP (Layer 7)
#---------------------------------------------------------------------
frontend http_front
    bind *:80
    mode http

    # Log all requests
    option httplog

    # ACL for path-based routing
    acl url_static path_beg -i /static /images /javascript /stylesheets
    acl url_static path_end -i .jpg .gif .png .css .js .ico

    # Use specific backend based on path
    use_backend static_servers if url_static

    # Default backend (HTTP)
    default_backend web_servers_http

    # Add custom headers
    http-request set-header X-Forwarded-Proto http
    http-response set-header X-Server-Group web-cluster

#---------------------------------------------------------------------
# Frontend - HTTPS (Layer 7) - SSL/TLS Termination
#---------------------------------------------------------------------
frontend https_front
    bind *:443 ssl crt /etc/haproxy/certs/haproxy.pem
    mode http

    # Log all requests
    option httplog

    # ACL for path-based routing
    acl url_static path_beg -i /static /images /javascript /stylesheets
    acl url_static path_end -i .jpg .gif .png .css .js .ico

    # Use specific backend based on path
    use_backend static_servers if url_static

    # Default backend (HTTPS)
    default_backend web_servers_https

    # Add custom headers
    http-request set-header X-Forwarded-Proto https
    http-response set-header X-Server-Group web-cluster
    http-response set-header Strict-Transport-Security "max-age=63072000"

#---------------------------------------------------------------------
# Backend - Web Servers (Round Robin)
#---------------------------------------------------------------------
backend web_servers_http
    mode http
    balance roundrobin

    # Health check
    option httpchk GET /health
    http-check expect status 200

    # Cookie-based persistence (optional)
    cookie SERVERID insert indirect nocache

    # Web server pool
    server web1 10.0.1.10:80 check cookie web1 inter 2000 rise 2 fall 3
    server web2 10.0.1.11:80 check cookie web2 inter 2000 rise 2 fall 3

    # Response headers
    http-response set-header X-Backend-Server %s

#---------------------------------------------------------------------
# Backend - Mixed Web Servers (HTTP + HTTPS backends)
#---------------------------------------------------------------------
backend web_servers_https
    mode http
    balance roundrobin

    # Health check
    option httpchk GET /health
    http-check expect status 200

    # Cookie-based persistence
    cookie SERVERID insert indirect nocache

    # HTTPS backend (web3) - SSL connection to backend
    server web3 10.0.1.12:443 check cookie web3 inter 2000 rise 2 fall 3 ssl verify none

    # Response headers
    http-response set-header X-Backend-Server %s

#---------------------------------------------------------------------
# Backend - Static Content (if needed)
#---------------------------------------------------------------------
backend static_servers
    mode http
    balance roundrobin

    # Health check
    option httpchk GET /health
    http-check expect status 200

    server web1 10.0.1.10:80 check
    server web2 10.0.1.11:80 check
    server web3 10.0.1.12:443 check ssl verify none

#---------------------------------------------------------------------
# TCP Mode Example - Layer 4 Load Balancing
#---------------------------------------------------------------------
# Uncomment to enable TCP mode (Layer 4)
# listen tcp_proxy
#     bind *:3306
#     mode tcp
#     balance roundrobin
#     server db1 10.0.1.30:3306 check
#     server db2 10.0.1.31:3306 check
